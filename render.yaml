services:
  - type: web
    name: finbot-ai-hub
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: DATA_DIR
        value: /opt/render/project/src/data
